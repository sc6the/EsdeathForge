package net.labymod.user.emote;

/**
 * Phase 1 placeholder. Phase 3 replaces this with the real ported emote animation
 * renderer (net.labymod.user.emote). LabyConnect only needs the type to exist as the
 * return of EmoteRegistry#handleEmote.
 */
public class EmoteRenderer {
}
