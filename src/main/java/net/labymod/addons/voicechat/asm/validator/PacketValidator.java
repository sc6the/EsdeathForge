package net.labymod.addons.voicechat.asm.validator;

public class PacketValidator {
   public static boolean isValidPacket(Object packet) {
      return packet instanceof Dummy ? true : packet instanceof Dummy;
   }
}
